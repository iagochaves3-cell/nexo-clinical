# Configure in Railway Variables; never commit real values.
NEXO_API_TOKEN=
# Optional, independent token restricted to capabilities/search/review.
NEXO_INTEGRATION_TOKEN=
# Optional OpenAI provider for a draft review; requires both fields.
NEXO_REVIEW_API_KEY=
NEXO_REVIEW_MODEL=
